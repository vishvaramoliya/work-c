// 3. Implement a function to reverse an array of characters in-place.

#include<stdio.h>
main(){

    char a[100];
    printf("Enter string :");
    gets (a);
    strrev (a);
    printf("reverse string = %s",a);

}